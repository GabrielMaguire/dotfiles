# Inserts a blank line between shell prompts
add_newline = false

format = """
$username\
$hostname\
$directory\
$git_branch\
$git_commit\
$git_state\
$git_status
> 
"""

[directory]
truncate_to_repo = false
truncation_length = 0
truncation_symbol = '…/'
